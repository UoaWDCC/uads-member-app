"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const ramda_1 = require("ramda");
const path = require('path');
const delineator = process.platform === 'win32' ? ';' : ':';
// Node mutates path by adding to the front, move that to the back if it exists
const originalPath = process.env.PATH || '';
const originalArray = originalPath.split(delineator);
const cleanArray = ramda_1.reject(ramda_1.contains('node_modules' + path.sep), originalArray);
process.env.PATH = ramda_1.concat(cleanArray, ramda_1.difference(originalArray, cleanArray)).join(delineator);
//# sourceMappingURL=quirksNodeModules.js.map