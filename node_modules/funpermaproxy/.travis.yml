sudo: false
language: node_js
node_js:
  - "node"
  - "8.3.0"

jobs:
  include:
    - stage: release
      node_js: "node"
      deploy:
        provider: script
        skip_cleanup: true
        script:
          - npx semantic-release
