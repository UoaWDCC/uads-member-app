import { Runtime } from '../runtime/runtime';
import { Command } from './command';
import { Options } from './options';
import { Plugin } from './plugin';
import { GluegunFilesystem, GluegunStrings, GluegunPrint, GluegunSystem, GluegunSemver, GluegunHttp, GluegunPatching, GluegunPrompt, GluegunTemplate, GluegunMeta, GluegunParameters } from '..';
export interface GluegunParameters {
    array?: string[];
    /**
     * Any optional parameters. Typically coming from command-line
     * arguments like this: `--force -p tsconfig.json`.
     */
    options?: Options;
    first?: string;
    second?: string;
    third?: string;
    string?: string;
    raw?: any;
    argv?: any;
    plugin?: string;
    command?: string;
}
export interface GluegunToolbox {
    result?: any;
    config?: Options;
    parameters: GluegunParameters;
    plugin?: Plugin;
    command?: Command;
    pluginName?: string;
    commandName?: string;
    runtime?: Runtime;
    filesystem?: GluegunFilesystem;
    http?: GluegunHttp;
    meta?: GluegunMeta;
    patching?: GluegunPatching;
    print?: GluegunPrint;
    prompt?: GluegunPrompt;
    semver?: GluegunSemver;
    strings?: GluegunStrings;
    system?: GluegunSystem;
    template?: GluegunTemplate;
    generate?: any;
    [key: string]: any;
}
export declare class Toolbox implements GluegunToolbox {
    [key: string]: any;
    result: any;
    config: Options;
    parameters: GluegunParameters;
    plugin: any;
    command: any;
    pluginName: any;
    commandName: any;
    runtime: any;
}
export declare type GluegunRunContext = GluegunToolbox;
export declare type RunContext = Toolbox;
